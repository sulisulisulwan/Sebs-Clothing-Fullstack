import React from 'react';

const CurrentProductSideBar = ({ parentClassName }) => {
  return (
    <div className={`${parentClassName}-sidebar-container`}>
      RIGHT SIDE BAR
    </div>
  )
}

export default CurrentProductSideBar;