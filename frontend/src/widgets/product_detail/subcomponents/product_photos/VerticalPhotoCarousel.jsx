import React from 'react';

const VerticalPhotoCarousel = ({ parentClassName }) => {
  return (
    <div className={`${parentClassName} vertical-photo-carousel-container`}>
      VERTICAL PHOTO CAROUSEL
    </div>
  )
};

export default VerticalPhotoCarousel;