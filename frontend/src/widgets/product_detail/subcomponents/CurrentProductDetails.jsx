import React from 'react';

const CurrentProductDetails = ({ parentClassName }) => {
  return (
    <div className={`${parentClassName}-current-product-details-container`}>
      CURRENT PRODUCT DETAILS
    </div>
  )
}

export default CurrentProductDetails;