import React from 'react';

const RelatedCards = ({ parentClassName }) => {

  return (
    <div className={`${parentClassName}-related-cards-container`}>
      RELATED CARDS
    </div>
  )
}

export default RelatedCards;