import React from 'react';

const QuestionText = ({ parentClassName }) => {

  return (
    <div className={`${parentClassName}-text`}>
      QUESTION TEXT
    </div>
  )
}

export default QuestionText;